VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional — error tracking (src/lib/sentry.js). Leave blank to disable.
VITE_SENTRY_DSN=

# Optional — Cloudflare Turnstile CAPTCHA on /register (src/components/shared/Turnstile.jsx).
# Leave blank to disable (register-user still works, just without bot protection).
# Pair with the TURNSTILE_SECRET_KEY edge function secret (supabase/functions/register-user).
VITE_TURNSTILE_SITE_KEY=
